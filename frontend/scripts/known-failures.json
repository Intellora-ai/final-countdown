{
  "_": "Fingerprint -> { reason, since }. A failure recorded here is classified FLAKE only when no environment or vendor tell claims it first. Add an entry only with the run that measured it and the reason it is probabilistic; remove it when the cause is fixed."
}
