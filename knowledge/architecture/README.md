# architecture

How this system actually works: components, data flow, boundaries.

Write a page here when someone would otherwise have to read five files to
answer "how does X work?". One page per component or flow. Name the files
after the thing, not the date.

State what is TRUE TODAY. If a page describes an intention rather than the
current code, say so in the first line, or it will be read as fact.
