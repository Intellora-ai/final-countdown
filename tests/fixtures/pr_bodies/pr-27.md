`honest_report.py:34` computes `score(spec_files, threshold)`. Line 35 then called `analyse(...)`, and `composition.py:11` opened with `joint = score(spec_files, threshold)` — **same function, same arguments, same process**, for a value the caller was already holding.

```
before   27.32s
after    21.36s
```

The duplicate was 3.74s of a 19.7s local run — **26 of its 142 Hypothesis searches**, spent recomputing a number that already existed.

## Proven, not argued

`comp` gates nothing: the blocking decision at `honest_report.py:41` reads `mut`, while `comp` feeds two printed lines that `run_gate.py`'s `_DIMENSION` extractor does not even scrape.

- same two specs, same command, before vs after → **byte-identical output**
- full four-function run → identical `Overall`, `Sufficiency`, `JOINT` lines

`analyse` takes an optional `joint`; passing nothing computes it exactly as before, so `check_composition.py` and every other caller are untouched.

## Second redundancy

`mutation_gate.py:135` read `any(...) and all(...)`. For non-empty `infos` — which `score()` guarantees, raising at line 100 on an empty set — `all` implies `any`. The `any` decided nothing and ran a second independent Hypothesis search per mutant whose result was discarded.

Stated precisely because `RUN` is **not** derandomized: this removes real random draws, not cached ones. It cannot change the verdict, but those searches were genuinely being performed and thrown away.

## No budget touched

`max_examples` stays 120. `DISTINGUISHER_EXAMPLES` stays 300. Nothing cached across runs, no verdict inherited, no Lean proof / kernel call / AXLE request skipped. The work removed is work whose result was discarded.

## Evidence

full suite exit 0 under `-n auto --dist loadfile` · byte-identical output · identical verdicts · pyright 0 · gate_integrity 0 · security_gate 0 · `diff --check` clean

**TCB:** three scripts stop recomputing results they already hold. No gate's exit code, threshold or judgement changes.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
